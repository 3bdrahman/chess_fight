[theme]
primaryColor = "#f0b421"
backgroundColor = "#0a0c14"
secondaryBackgroundColor = "#11141f"
textColor = "#e8eaf2"
font = "monospace"

[server]
# Use polling watcher to avoid inotify watch limit on Streamlit Cloud
fileWatcherType = "poll"
